交互设计应避免让用户思考。以下是将业务逻辑转化为用户视角的流转指南：
**1. 基础布局与导航流**

- **全局框架 (Bottom/Side Navigation):** 左侧【个人中心】抽屉或 Tab，中间主视觉【地图引擎】，右侧【设置】悬浮球或面板。
- **页面流转:**`启动屏` -> `权限申请授权弹窗` -> `主地图页` -> (若未登录点击高阶功能) -> `登录/注册 Bottom Sheet 弹窗` -> `主地图页`。
	**2. 核心场景：地图探索与音频播放**

- **场景 1：自动触发播放**
	- **交互动作：** 用户持手机走入景点 A 电子围栏范围。
	- **视觉反馈：** 界面底部弹出【景点 A 介绍卡片】（包含默认图片、骨架屏过渡到真实文本）。
	- **状态反馈：** 首次触发时，叠加一个模态弹窗（Modal）：“检测到您已到达，是否开启自动讲解？[手动点播] / [开启自动]”。
- **场景 2：音频控制面板**
	- **交互动作：** 用户拖拽进度条、切换倍速（1.0x -> 1.2x -> 1.5x -> 2.0x -> 1.0x 循环）、点击“AI 讲演”。
	- **视觉反馈：** 进度条需有丝滑的吸附感；倍速切换时有轻微的触觉震动（Haptic Feedback）；播放器处于活跃状态时，地图上的当前景点地标应有呼吸灯或水波纹动效（Ripple Effect）。
- **场景 3：游客碰壁与转化**
	- **交互动作：** 游客点击“播放图标”或“搜索框”。
	- **视觉反馈：** 触发 Toast 提示：“当前为游客模式，无法使用该功能”，并在 0.5s 后向上滑出【快速登录面板】。
		**3. 异常与缺省状态反馈**

- **无定位权限：** 地图中心显示灰色占位符，顶部悬浮醒目的红色 Banner：“📍 无法获取位置，请在系统设置中开启定位以便体验智能导览”，附带【去设置】按钮。
- **搜索无结果：** 搜索面板展示缺省页插画（如拿着放大镜的迷茫小人），文案提示：“未找到相关地点，请换个词试试”。
- **弱网状态：** 音频加载时，播放按钮变为转圈的 Loading 动画；若超时，Toast 提示：“网络不佳，音频加载失败，请重试”。

#### 模块一：管理员界面 (Admin Management Workspace)

由于管理员侧重点在“高效数据维护”，UI 设计应采用结构化的 B 端低噪风格，聚焦于信息密度和防错机制。

- **页面/弹窗流转：**
	- `管理员登录` -> `进入管理工作台（列表页）` -> `点击单条景点` -> `右侧抽屉滑出（详情与编辑页）` -> `保存/取消`。
- **关键交互动作：**
	- **列表浏览：** 界面采用纵向列表排布，仅展示景点核心文本（名称、简介片段、上次修改人、修改时间）。支持快速滚动与关键词检索。
	- **信息修改：** 点击列表行，右侧平滑滑出【编辑抽屉】。管理员可在表单中直接修改：经纬度、感应范围（米）、介绍文本。
- **视觉与状态反馈：**
	- **轻量化审计态：** 列表页的“上次修改人”和“修改时间”采用低对比度字色（如中灰色），作为辅助信息流，不干扰核心文本。
	- **表单校验：** 经纬度输入框必须限制只能输入数字与小数点，格式不符时输入框失焦高亮飘红，提示“请输入正确的地理坐标格式”。
	- **防误触与防丢失：** 当管理员修改了表单内容但未点击保存，直接点击空白处关闭抽屉时，必须触发模态二次确认弹窗：“内容已修改，是否放弃保存？[放弃] / [继续编辑]”。
	- **保存反馈：** 点击保存按钮，按钮进入 Loading 状态；成功后抽屉收回，顶部弹出绿色 Toast：“修改成功”，列表对应行数据完成局部刷新（无闪烁刷新）。

#### 模块二：个人信息界面 (User Profile Hub)

此界面为 C 端常规入口，设计原则是“极简、安全、流畅”。

- **页面/弹窗流转：**
	- `主地图页` -> `点击左上角头像` -> `个人中心（左侧抽屉滑出）` -> `点击修改密码` -> `修改密码全屏页/弹窗`。
- **关键交互动作：**
	- 用户点击【个人中心】查看当前状态（默认头像、当前登录的手机号）。
	- 点击【修改密码】按钮，进入密码变更表单。
- **视觉与状态反馈：**
	- **安全脱敏：** 当前电话号码不得全文明文显示，界面应进行脱敏处理（例如：`138****8888`），保护用户隐私。
	- **默认状态：** 若用户未上传自定义头像，系统统一展示标准的品牌默认剪影头像（或带有品牌色调的 Icon）。
	- **密码变更反馈：** 修改密码界面包含三个输入框（原密码、新密码、确认新密码）。输入框右侧提供“眼睛”图标切换明暗文。当两次新密码输入不一致时，实时在下方渲染红字提示。
	- **强制重登流转：** 点击“确认修改”成功后，弹出全局强提示 Toast：“密码修改成功，请重新登录”，1.5s 后界面自动清空本地 Token，平滑淡出并跳转回【登录界面】。

#### 模块三：自动播放设置界面 (Smart Audio Settings)

该界面决定了用户核心“边走边听”的沉浸式体验，属于高阶配置，交互上需要极强的联动逻辑与状态控制。

- **页面/弹窗流转：**
	- `主地图页` -> `点击右侧设置悬浮球` -> `自动播放设置面板（从底部滑出 Bottom Sheet）`。
- **关键交互动作：**
	- **总开关：** 手动开启/关闭“自动播放”控制。
	- **策略一（频次）：** 单选框（Radio）切换“每个景点只播放一次”或“可以播放多次”。
	- **策略二（触发）：** 三选一单选框切换“播完再切”、“随位置实时切”或“增加弹窗提醒”。
	- **系统级权限：** 开启“后台播放设置”开关。
- **视觉与状态反馈：**
	- **依赖联动（Disabled 状态）：** 当用户关闭最上方的“手动开启自动播放”总开关时，下方所有的子配置项（重复播放、播放切换、提醒设置）整体色彩变灰，并处于不可点击状态（Disabled），同时伴有渐变动画，避免界面元素过多对用户产生心智负担。
	- **“增加提醒”的场景联动：** 如果用户在播放切换中选择了“增加提醒”，在实际导览中触发时，UI 不得直接生硬地切音频，而是必须在地图主页中央弹出一个悬浮半透明通知卡片：“📍 检测到您已到达 B 点，是否切换至 B 点讲解？[切换] / [忽略]”，保持用户的控制权。
	- **后台权限引导流：** 用户开启“后台播放设置”时，前端需立即检测系统权限。若检测到无“始终允许访问位置”或“后台刷新”权限，立即中断开关开启，弹出引导弹窗：“需要系统后台定位权限以保证锁屏时正常讲解，请在设置中开启”，提供【去开启】的一键跳转系统设置按钮。若用户拒绝，开关自动回弹至关闭状态。